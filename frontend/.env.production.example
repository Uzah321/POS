# Frontend production environment
# Offline desktop builds must use the bundled local Laravel server.
# Leave VITE_API_URL blank so the app calls /api relative to Core.exe's local window.
VITE_API_URL=

VITE_APP_NAME=NexaPOS
